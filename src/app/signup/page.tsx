import * as Section from "@/components/section";
export default async function Page() {
  return (
    <Section.Root>
      <Section.Container>
        <Section.Header>
          <Section.Title>Cadastro</Section.Title>
        </Section.Header>
      </Section.Container>
    </Section.Root>
  );
}
